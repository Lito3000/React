import React from "react";

const Footer = (props) =>
         <div className="modal-footer">
            <button type="button" className="modal-close-button btn btn-secondary" onClick={props.ContentJsx.toggle}>Cancel</button>
        </div>

export default Footer;